import React from "react";

// 로그아웃 api 연결
const LogoutItem = () => {
  return (
    <button className="text-sm px-[10px] py-[5px] rounded-[5px] hover:bg-subColor">로그아웃</button>
  );
};

export default LogoutItem;
